"""Prediction logging helpers."""


def log_demo_result(image_path: str, question: str, answer: str, question_type: str, inference_time_sec: float):
    """Append a demo prediction row to data/predictions.csv."""
    raise NotImplementedError("Demo logging will be implemented during UI integration.")
